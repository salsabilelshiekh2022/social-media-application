import 'package:flutter/material.dart';

import 'widgets/forgot_passsword_method_body.dart';

class ForgotPasswordMethodView extends StatelessWidget {
  const ForgotPasswordMethodView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ForgotPasswordMethodBody(),
    );
  }
}
